# System Spec — 01: Features

> Actualizado automaticamente após cada Issue pela Fase 3 (documenta-issue).

## Features planeadas (src/app/features/)

| Feature          | Componentes planeados                                        | Rota         | Estado   |
| ---------------- | ------------------------------------------------------------ | ------------ | -------- |
| `dashboard`      | DashboardComponent (KPIs, gráficos)                         | `/`          | pendente |
| `documents`      | DocumentTable, DocumentRow, DocumentFilters, StatusBadge    | `/documents` | pendente |
| `upload`         | UploadZoneComponent, UploadProgressComponent                | `/documents` | pendente |
| `file-explorer`  | FileExplorerComponent, FileTreeNode, ContextMenu            | `/documents` | pendente |
| `correct-modal`  | CorrectModalComponent (ReactiveForm tipado)                  | modal        | pendente |
| `activity-log`   | ActivityLogComponent                                        | `/documents` | pendente |
| `config`         | ConfigComponent, ExtractionTemplatesComponent               | `/config`    | pendente |

_Vazio até à primeira issue implementada._
