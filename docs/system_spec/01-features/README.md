# Features

_Nenhuma feature implementada ainda._

Cada feature ganha aqui um `<slug>.md` **quando for construída** (via `atualiza-spec`, no
`/documenta-implementacao`) — descrevendo componentes, rota, camadas consumidas e regras reais.
Não se antecipam features/componentes especulativos no spec (regra "refletir só o que existe").

> Ao criar o primeiro ficheiro, adicionar a linha correspondente em `../00-index.md`.
